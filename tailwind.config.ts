import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: ["class"],
  content: [
    "./src/pages/**/*.{ts,tsx}",
    "./src/components/**/*.{ts,tsx}",
    "./src/app/**/*.{ts,tsx}",
  ],
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: { "2xl": "1400px" },
    },
    extend: {
      colors: {
        // Loyiha palitrasi (CSS o'zgaruvchilar orqali — globals.css)
        navy: {
          DEFAULT: "var(--navy)",
          mid: "var(--navy-mid)",
        },
        cobalt: "var(--cobalt)",
        gold: {
          DEFAULT: "var(--gold)",
          light: "var(--gold-light)",
        },
        // shadcn-uslubidagi semantik ranglar
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        primary: {
          DEFAULT: "var(--navy)",
          foreground: "#ffffff",
        },
        secondary: {
          DEFAULT: "var(--gold)",
          foreground: "var(--navy)",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
    },
  },
  plugins: [],
};

export default config;

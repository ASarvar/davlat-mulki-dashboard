# Build kontekstidan chiqariladi — image kichik va sirlar ichkariga tushmaydi.

node_modules
.next
out
build

# Sirlar — runtime env compose'dagi env_file orqali beriladi
.env
.env.*
!.env.example
!.env.production.example

# Yuklangan hujjatlar (server volume'ida yashaydi)
data
uploads

# ⚠️ Baza zaxiralari. Server papkasida `db-2026-08-17.sql.gz` kabi fayllar
# turgan edi va ular build kontekstiga kirardi: `Dockerfile` da `COPY . .`
# bor, `tools` bosqichi esa `builder` dan meros oladi — ya'ni BUTUN baza
# dump'i ishlab turgan worker image'i ichida qolardi.
*.sql
*.sql.gz
*.dump
*.tar.gz

# Git / IDE / OS
.git
.gitignore
.gitattributes
.vscode
.idea
.DS_Store
Thumbs.db

# Claude / vaqtinchalik
.claude
_*.ts
*.log
npm-debug.log*

# Hujjatlar (image ichida keraksiz)
*.md
!README.md

# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
# production sirlari (serverda qo'lda yaratiladi) — namunasi git'da qoladi
.env.production
!.env.production.example
# auksion buyurtmalari API'si — 14 viloyatning login/paroli JSON bo'lib turadi.
# ⚠️ 2026-09-07 dan kalitlar `.env` ga ko'chirildi; qoida ZAXIRA/NUSXA fayllar
# uchun qoldirildi (`.env.auction.bak` bir marta ignore'ga tushmay qolgan edi).
.env.auction*
!.env.auction.example

# vaqtinchalik diagnostika/sinov skriptlari (_diag.ts, _scope.ts ...) — repo'ga tushmasin
/_*.ts

# prisma
/prisma/*.db

# uploads (local filesystem PDF saqlash)
/data/
/uploads/

# typescript
*.tsbuildinfo
